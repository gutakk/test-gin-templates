name: Integration test
on: push

env:
  TEST: ${{ secrets.DATABASE_URL_STAGING }}

jobs:
  integ_test:
    name: "Integration test"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Echo ENV
        run: echo $TEST
